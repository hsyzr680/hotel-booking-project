# 🏨 دليل تثبيت وتشغيل موقع حجز الفنادق

## 📋 نظرة عامة

نظام حجز فنادق احترافي مبني بأحدث التقنيات:
- **Next.js 14** - أسرع وأقوى framework
- **TypeScript** - للكود الآمن والمنظم
- **SQLite** - قاعدة بيانات سهلة وسريعة
- **NextAuth** - نظام مصادقة آمن
- **Tailwind CSS** - تصميم حديث ومتجاوب

---

## ✨ المميزات الكاملة

### للزوار والعملاء:
- ✅ البحث عن الفنادق (بالمدينة، التاريخ، عدد الضيوف)
- ✅ فلترة متقدمة (النجوم، السعر)
- ✅ عرض تفاصيل كاملة للفنادق
- ✅ نظام حجز سهل وسريع
- ✅ حساب شخصي لعرض الحجوزات
- ✅ التقييمات والمراجعات

### للإدارة:
- ✅ لوحة تحكم احترافية
- ✅ إحصائيات شاملة
- ✅ إدارة الفنادق والغرف
- ✅ إدارة الحجوزات
- ✅ إدارة المستخدمين
- ✅ عرض التقييمات

---

## 🚀 التثبيت السريع (5 دقائق)

### الخطوة 1: المتطلبات
- **Node.js** إصدار 18 أو أحدث
- محرر نصوص (VS Code مُفضل)

### الخطوة 2: تثبيت المكتبات
```bash
npm install
```

### الخطوة 3: إعداد البيئة
انسخ ملف `.env.example` إلى `.env`:
```bash
cp .env.example .env
```

### الخطوة 4: إعداد قاعدة البيانات
```bash
npx prisma generate
npx prisma db push
npm run db:seed
```

### الخطوة 5: تشغيل المشروع
```bash
npm run dev
```

افتح المتصفح على: **http://localhost:3000**

---

## 🔐 حسابات الدخول الافتراضية

### حساب المدير:
- البريد: `admin@hotel.com`
- كلمة المرور: `admin123`

### حساب مستخدم:
- البريد: `user@test.com`
- كلمة المرور: `user123`

**⚠️ مهم:** غيّر هذه الحسابات في الإنتاج!

---

## 📂 هيكل المشروع

```
hotel-booking/
├── prisma/
│   ├── schema.prisma      # تعريف قاعدة البيانات
│   └── seed.ts            # البيانات التجريبية
├── src/
│   ├── app/               # صفحات Next.js
│   │   ├── page.tsx       # الصفحة الرئيسية
│   │   ├── search/        # صفحة البحث
│   │   ├── hotel/         # تفاصيل الفندق
│   │   ├── booking/       # نظام الحجز
│   │   ├── auth/          # تسجيل الدخول
│   │   ├── admin/         # لوحة الإدارة
│   │   └── api/           # API Routes
│   ├── components/        # المكونات القابلة لإعادة الاستخدام
│   │   ├── ui/            # مكونات الواجهة
│   │   ├── layout/        # Header & Footer
│   │   ├── hotel/         # مكونات الفنادق
│   │   └── search/        # مكونات البحث
│   ├── lib/               # المكتبات والأدوات
│   └── types/             # أنواع TypeScript
└── public/                # الملفات الثابتة
```

---

## 🗄️ قاعدة البيانات

### الجداول:
1. **Users** - المستخدمين (Admin/User)
2. **Hotels** - الفنادق
3. **Rooms** - الغرف
4. **Bookings** - الحجوزات
5. **Reviews** - التقييمات
6. **Favorites** - المفضلة

### تغيير قاعدة البيانات:
حالياً تستخدم SQLite (ملف محلي). لتغييرها إلى PostgreSQL أو MySQL:

1. عدّل `prisma/schema.prisma`:
```prisma
datasource db {
  provider = "postgresql"  // أو mysql
  url      = env("DATABASE_URL")
}
```

2. غيّر `DATABASE_URL` في `.env`
3. نفذ:
```bash
npx prisma generate
npx prisma db push
```

---

## 🎨 التخصيص

### تغيير الألوان:
عدّل `tailwind.config.ts`:
```typescript
colors: {
  primary: {
    500: '#0ea5e9', // اللون الأساسي
  }
}
```

### تغيير الخط:
عدّل `src/app/layout.tsx`:
```typescript
import { Cairo } from 'next/font/google'
const cairo = Cairo({ subsets: ['arabic'] })
```

### إضافة لغة:
المشروع حالياً يدعم العربية فقط. لإضافة الإنجليزية:
1. استخدم `next-intl` أو `i18next`
2. أنشئ ملفات ترجمة في `locales/`

---

## 🚢 النشر (Deployment)

### Vercel (موصى به - مجاني):
```bash
npm install -g vercel
vercel
```

### خطوات النشر:
1. ارفع المشروع على GitHub
2. اربطه مع Vercel
3. أضف متغيرات البيئة:
   - `DATABASE_URL`
   - `NEXTAUTH_SECRET`
   - `NEXTAUTH_URL`

### قواعد بيانات الإنتاج (اختر واحد):
- **Vercel Postgres** (سهل ومتكامل)
- **PlanetScale** (MySQL مجاني)
- **Supabase** (PostgreSQL مجاني)
- **Railway** (PostgreSQL/MySQL)

---

## 🔧 الإعدادات المهمة

### 1. تغيير NEXTAUTH_SECRET:
```bash
openssl rand -base64 32
```
ضع النتيجة في `.env`

### 2. إضافة نطاق (Domain):
غيّر `NEXTAUTH_URL` في `.env`:
```
NEXTAUTH_URL="https://yourdomain.com"
```

### 3. إضافة Stripe للدفع:
1. سجل في Stripe.com
2. احصل على API Keys
3. أضفها في `.env`:
```
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
STRIPE_SECRET_KEY="sk_live_..."
```

---

## 📊 الإحصائيات

- **38 ملف** مكتوب يدوياً
- **6 جداول** في قاعدة البيانات
- **10+ صفحة** ويب
- **30+ مكون** React
- **100% TypeScript**
- **كود نظيف ومنظم**

---

## 🆘 الدعم

### مشاكل شائعة:

**المشكلة:** `npm install` يعطي أخطاء
**الحل:** تأكد من تثبيت Node.js 18+

**المشكلة:** قاعدة البيانات لا تعمل
**الحل:** شغّل `npx prisma db push`

**المشكلة:** NextAuth لا يعمل
**الحل:** تأكد من `NEXTAUTH_SECRET` في `.env`

**المشكلة:** الصور لا تظهر
**الحل:** تأكد من إعدادات `next.config.js`

---

## 📄 الترخيص

هذا المشروع جاهز للاستخدام التجاري.
يمكنك:
- ✅ البيع
- ✅ التعديل
- ✅ إعادة التوزيع
- ✅ الاستخدام التجاري

---

## 🎯 التطويرات المستقبلية (اقتراحات)

1. إضافة دفع حقيقي (Stripe/PayPal)
2. تطبيق موبايل (React Native)
3. دعم اللغة الإنجليزية
4. نظام الإشعارات (Push Notifications)
5. تكامل مع خرائط Google
6. نظام الكوبونات والخصومات
7. برنامج نقاط الولاء
8. تقارير Excel/PDF

---

## ✅ Checklist للإنتاج

قبل النشر، تأكد من:
- [ ] تغيير كلمات مرور الحسابات الافتراضية
- [ ] تغيير NEXTAUTH_SECRET
- [ ] إعداد قاعدة بيانات إنتاج
- [ ] إضافة Google Analytics (اختياري)
- [ ] اختبار جميع المميزات
- [ ] إضافة صور حقيقية للفنادق
- [ ] مراجعة النصوص والترجمة
- [ ] إعداد النسخ الاحتياطي

---

**المشروع جاهز للعمل 100%!** 🎊

لأي استفسارات أو مساعدة، الكود واضح ومنظم وسهل الفهم.
