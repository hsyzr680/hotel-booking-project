# 🔐 كيف تحصل على المتغيرات الثلاثة

## ✅ الملف `.env.local` جاهز!

تم إنشاء الملف بالمتغيرات الثلاثة، لكن تحتاج تعديل واحد فقط:

---

## 1️⃣ DATABASE_URL (يحتاج تعديل) ⚠️

### الحالة الحالية:
```env
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
```
**❌ هذا مجرد مثال ولن يعمل!**

---

### كيف تحصل على DATABASE_URL الحقيقي:

#### الخيار أ: Supabase (مجاني، موصى به) ⭐

**الخطوات:**

1. **افتح المتصفح واذهب لـ:**
   ```
   https://supabase.com
   ```

2. **سجل دخول:**
   - اضغط "Start your project"
   - اختر "Continue with GitHub"
   - سجل دخول بحساب GitHub

3. **أنشئ مشروع جديد:**
   - اضغط "New Project"
   - املأ البيانات:
     ```
     Name: hotel-booking
     Database Password: اختر كلمة سر قوية (احفظها!)
     Region: اختر الأقرب لبلدك
     ```
   - اضغط "Create new project"
   - انتظر 2-3 دقائق حتى يجهز المشروع

4. **احصل على Connection String:**
   - بعد انتهاء الإنشاء
   - اضغط على أيقونة الإعدادات (⚙️)
   - اختر "Database"
   - انزل لقسم "Connection String"
   - اختر "URI" (وليس Transaction)
   - اضغط "Copy"
   
5. **عدّل الرابط:**
   - الرابط سيكون بهذا الشكل:
     ```
     postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-xx.pooler.supabase.com:5432/postgres
     ```
   - **مهم:** غيّر `[YOUR-PASSWORD]` بكلمة المرور التي اخترتها في الخطوة 3

6. **ضع الرابط في `.env.local`:**
   - افتح ملف `.env.local`
   - ابحث عن السطر:
     ```env
     DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
     ```
   - استبدله بالرابط الحقيقي:
     ```env
     DATABASE_URL="postgresql://postgres.xxxxx:YOUR-PASSWORD@aws-0-xx.pooler.supabase.com:5432/postgres"
     ```

---

#### الخيار ب: Neon (مجاني، سريع) 🚀

**الخطوات:**

1. **اذهب لـ:**
   ```
   https://neon.tech
   ```

2. **سجل دخول:**
   - اضغط "Sign up"
   - اختر "Continue with GitHub"

3. **أنشئ مشروع:**
   - اضغط "Create a project"
   - سيُنشأ تلقائياً
   - مباشرة سترى "Connection String"

4. **انسخ الرابط:**
   - انسخ الرابط كما هو
   - ضعه في `.env.local` مكان `DATABASE_URL`

---

## 2️⃣ NEXTAUTH_SECRET (جاهز!) ✅

### الحالة الحالية:
```env
NEXTAUTH_SECRET="7K9mNpR2tX5wY8zB3cD6fG9hJ2kL5nP8qS1tV4wX7zA0bC3eF6gH9jK2mN5pR8s"
```

**✅ هذا المفتاح جاهز ويعمل!**
**✅ لا تحتاج تعديله!**

لكن إذا أردت توليد مفتاح جديد:

### طريقة 1: موقع Generate Secret
```
1. اذهب لـ: https://generate-secret.vercel.app/32
2. اضغط "Generate"
3. انسخ المفتاح
4. ضعه مكان NEXTAUTH_SECRET
```

### طريقة 2: PowerShell (على Windows)
```powershell
# افتح PowerShell واكتب:
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
```

---

## 3️⃣ NEXTAUTH_URL (جاهز للمحلي!) ✅

### الحالة الحالية:
```env
NEXTAUTH_URL="http://localhost:3000"
```

**✅ هذا جاهز للتطوير المحلي!**

### للنشر على Vercel:
عند النشر على Vercel، غيّره إلى:
```env
NEXTAUTH_URL="https://your-project-name.vercel.app"
```

**كيف تعرف رابط مشروعك؟**
1. اذهب لـ Vercel Dashboard
2. افتح مشروعك
3. ستجد الرابط في الأعلى

---

## 📋 ملخص سريع:

| المتغير | الحالة | الإجراء |
|---------|--------|----------|
| `DATABASE_URL` | ❌ يحتاج تعديل | احصل عليه من Supabase أو Neon |
| `NEXTAUTH_SECRET` | ✅ جاهز | لا تحتاج فعل شيء |
| `NEXTAUTH_URL` | ✅ جاهز | جاهز للمحلي، غيّره عند النشر |

---

## 🚀 الخطوات التالية (بعد تعديل DATABASE_URL):

### 1. أنشئ جداول قاعدة البيانات:
```bash
npx prisma db push
```

### 2. أضف بيانات تجريبية:
```bash
npm run db:seed
```

### 3. شغّل المشروع:
```bash
npm run dev
```

### 4. افتح المتصفح:
```
http://localhost:3000
```

**تسجيل دخول:**
- Email: `admin@test.com`
- Password: `admin123`

---

## 📝 ملاحظات مهمة:

### ⚠️ لا ترفع `.env.local` على GitHub!
الملف في `.gitignore` تلقائياً - لا تحذفه من هناك!

### ⚠️ احفظ DATABASE_URL في مكان آمن!
احفظه في مدير كلمات المرور أو ملف نصي محلي.

### ⚠️ استخدم نفس المتغيرات في Vercel!
عند النشر على Vercel، ضع نفس القيم في:
```
Vercel → Settings → Environment Variables
```

---

## 🎯 المتغيرات في Vercel:

عند النشر، ضع هذه القيم **بالضبط** في Vercel:

```
DATABASE_URL = <نفس قيمة .env.local>
NEXTAUTH_SECRET = <نفس قيمة .env.local>
NEXTAUTH_URL = https://your-project.vercel.app
```

**ملاحظة:** `NEXTAUTH_URL` فقط يختلف (Vercel بدلاً من localhost)

---

## 🆘 مشاكل شائعة:

### "Can't reach database server"
**السبب:** DATABASE_URL غير صحيح
**الحل:** 
1. تحقق من الرابط
2. تأكد أنك غيّرت `[YOUR-PASSWORD]` بكلمة المرور الحقيقية
3. جرّب الاتصال في Supabase Dashboard

### "NEXTAUTH_SECRET is undefined"
**السبب:** الملف `.env.local` غير موجود أو غير محمّل
**الحل:**
1. تأكد من وجود الملف في جذر المشروع
2. أعد تشغيل الخادم: `npm run dev`

### "Prisma Client not found"
**الحل:**
```bash
npx prisma generate
npx prisma db push
```

---

## 📞 خطوات سريعة:

```
1. [ ] سجل في Supabase/Neon
2. [ ] أنشئ قاعدة بيانات
3. [ ] انسخ Connection String
4. [ ] ضعه في .env.local
5. [ ] npx prisma db push
6. [ ] npm run db:seed
7. [ ] npm run dev
8. [ ] افتح localhost:3000
9. [ ] 🎉 يعمل!
```

---

**🎉 الآن ملف `.env.local` جاهز!**
**فقط عدّل `DATABASE_URL` وابدأ التطوير!**
